// *individual medication component view

// TODO on click - renders update, delete and hold buttons
// TODO on click - renders details in display componenet (quantity, refill date, prescribing doctor, interactions)

// displays medication name, dose, sx to be treated, and time of day to administer with frequency
// displays day of week with large char for day of week
// if medication quantity exceed viewport, add scroll feature

export default function MedicationListView () {
  return (
    <div>
      Medication List!
    </div>
  )
}