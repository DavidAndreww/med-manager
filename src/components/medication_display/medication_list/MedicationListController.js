// * MedicationListController Component

// TODO functionality to remove medication from list
// ? deleteMedFunc => identify the clicked medication by its rxcui
// ? remove that medication from the medicationModel array of meds

// TODO funtionality to update medication details
// ? updateMedFunc => identify clicked medication and update medication details
// ? (input fields )

// TODO functionality to render medications prescribed by selected doctor
// ? handleDoctorChange -parentFx- => passed in from DisplayController to update visible
// ?                       medications when user selects new doctor

// TODO functionality to display medication details when med is selected
// ? displayMedDetails -redux- => renders medication details in details
// ?                              display component
