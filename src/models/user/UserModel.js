// * User class object

/*
function User (props) {
  this.medList = []
  this.doctorList = []
}
*/ 

// ? User.prototype.addMedicationFunc => adds medication object to medicationList. 
// TODO renders new medication to medicationDisplay
// TODO checks for medication interactins with other currently prescribed meds (adds indicator to medicationView if interaction is present)
// TODO 

// ? User.prototype.removeMedicationFunc => deletes medication information

// ? User.prototype.addDoctor => adds new doctor to doctorList

// ? User.prototype.removeDoctor => removes doctor from doctorList

// ? User.prototype.checkInteractions => checks for adverse interactions among current medications 

