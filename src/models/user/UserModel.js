// * User class object

/*
  this.medicationList = {
    sunday: [],
    monday: [],
    tuesday: [],
    wednesday: [],
    thursday: [],
    friday: [],
    saturday: []
  }
 * /

/*
  this.doctorList = []
*/ 

// ? addMedicationFunc => adds medication object to medicationList. 
// TODO renders new medication to medicationDisplay
// TODO checks for medication interactins with other currently prescribed meds (adds indicator to medicationView if interaction is present)
// TODO 

// ? removeMedicationFunc => edits medication information

// ? addDoctor  => adds new doctor to doctorList

// ? removeDoctor => removes doctor from doctorList

// ? checkInteractions => checks for adverse interactions among current medications 